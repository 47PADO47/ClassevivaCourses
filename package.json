{
  "scripts": {
    "publish": "npx wrangler pages deploy . --project-name=webcvv --branch=production",
    "preview": "npm run build && npm run config:fill && npx wrangler pages deploy .  --branch=production --project-name=webcvv --commit-dirty=true",
    "build": "npx tsc && node scripts/format.js && node scripts/listCourses.js",
    "config:fill": "node scripts/fillConfig.js"
  },
  "devDependencies": {
    "tslib": "^2.4.1",
    "typescript": "^4.9.4"
  }
}
